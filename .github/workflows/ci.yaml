name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Login Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build & Push housing-api
      - name: Build and Push score-api image
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          DOCKER_REPO=${{ secrets.DOCKERHUB_USERNAME }}/score-api

          docker build -t $DOCKER_REPO:$IMAGE_TAG -f Dockerfile .
          docker push $DOCKER_REPO:$IMAGE_TAG

          docker tag $DOCKER_REPO:$IMAGE_TAG $DOCKER_REPO:latest
          docker push $DOCKER_REPO:latest

      # Build & Push housing-streamlit
      - name: Build and Push housing-streamlit image
        run: |
          IMAGE_TAG=${GITHUB_SHA}
          DOCKER_REPO=${{ secrets.DOCKERHUB_USERNAME }}/score-streamlit

          docker build -t $DOCKER_REPO:$IMAGE_TAG -f Dockerfile.streamlit .
          docker push $DOCKER_REPO:$IMAGE_TAG

          docker tag $DOCKER_REPO:$IMAGE_TAG $DOCKER_REPO:latest
          docker push $DOCKER_REPO:latest